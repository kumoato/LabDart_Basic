void main(List<String> args) {
  String title = 'Dart course';
  print(title);
  title = title.toUpperCase();
  print(title);
  title = title.toLowerCase();
  print(title);

  // Desired ourput:
  // Dart course
  // DART COURSE
  // dart course
}